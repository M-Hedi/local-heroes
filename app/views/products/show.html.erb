<% content_for :title, "#{@product.name} - #{@product.store.name} | Local Heroes" %>

<div class="container mx-auto ">

  <!-- Titre -->
  <h1 class="bg-orange-100 rounded-[10px] p-[15px] mt-[30px] text-xl font-bold text-center"><%= @product.name %></h1>

  <!-- Carte principale -->
  <%# to do show product %>
</div>
